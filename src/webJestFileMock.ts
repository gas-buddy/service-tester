// Stub returned by webJestConfig's moduleNameMapper for static asset imports
// (svg/png/jpg/gif) so importing them in a component under test doesn't
// require an actual file-loader/webpack pipeline.
export default 'test-file-stub';
